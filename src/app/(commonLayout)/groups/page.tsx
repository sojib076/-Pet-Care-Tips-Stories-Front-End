import JoinGroups from '@/app/(dashboardLayout)/(userDashboard)/dashboard/components/DiscoverGroups';
import React from 'react';

const page = () => {
    return (
        <div>
            <JoinGroups></JoinGroups>
        </div>
    );
};

export default page;