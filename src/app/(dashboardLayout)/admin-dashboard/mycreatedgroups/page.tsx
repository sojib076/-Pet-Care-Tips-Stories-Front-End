import React from 'react';
import MyCreatedGroups from '../../(userDashboard)/dashboard/components/MyCreatedGroups';

const page = () => {
    return (
        <div>
            <MyCreatedGroups />
        </div>
    );
};

export default page;